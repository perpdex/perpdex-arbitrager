# perpdex-arbitrager-hardhat
this project uses hardhat-deploy plugin
* https://github.com/wighawag/hardhat-deploy#2-hardhat-node

To run stand-alone hardhat:
```
yarn install
yarn hardhat node --network localhost
```
